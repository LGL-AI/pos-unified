# Đối chiếu 4 UI / D1 · Lotus POS 2.5.0

Các API ghi và đọc qua cùng Worker/D1; [156 tình huống có tên](tests/cross-ui-70.test.mjs) chạy với SQLite áp đủ migrations 0001–0008. Quầy/POS/QR lấy danh mục từ `/api/catalog`; màn hình thứ hai lấy snapshot từ phiên ghép D1.

| Chủ thể / thao tác | Nơi lưu | POS quầy và POS cầm tay | QR của khách | Màn hình thứ hai |
| --- | --- | --- | --- | --- |
| Chủ tiệm sửa thông tin quán, logo, bàn | `pos_store_config`; logo PNG đọc qua `/api/store/logo` | Quầy mở **Quản lý tiệm** để chỉnh, danh sách bàn có giới hạn mới | Tên/logo/bàn được đồng bộ khi lấy lại catalog; bàn vượt giới hạn bị Worker chặn | Lấy tên/logo mới và giỏ khi phiên quầy cập nhật |
| Chủ tiệm đổi ngân hàng, tiền tố, phương pháp/thuế suất | `pos_store_config` cho đơn mới; `qr_orders` giữ bản chụp `bank_*`, `tax_*`, `transfer_prefix` | Quầy chỉnh; cả hai POS đọc đúng QR tiền mỗi đơn/bill, kể cả bill đã tách/gộp | Đơn đã tạo giữ nguyên tài khoản/số tiền; khách tải PNG đúng mã đơn | Số tiền giỏ nháp theo thuế hiện hành; đơn đã chốt theo D1 |
| Quầy tạo/sửa món, voucher và nhập kho | `pos_products`, `vouchers`, `pos_inventory_*`, `pos_ingredients` | Mọi POS lấy giá/kho/quy tắc mới; đơn cũ giữ giá/ưu đãi đã chốt | Nếu còn món mà hết nguyên liệu thì vẫn báo hết; đủ cả hai mới mở **Thêm được** | Giỏ nháp quầy được Worker định giá lại |
| QR hoặc POS chốt/thêm/hủy | `qr_orders`, `pos_kitchen_jobs`, tồn kho và voucher | Đơn đến hai POS; bếp nhận phiếu món mới, không cộng lặp cũ | Token riêng xem đơn, trạng thái và số tiền đã chốt | Phiên ghép quầy hiển thị giỏ, giá và trạng thái mới |
| Tách bill/gộp/thu tiền/hoàn | `pos_bills`, `qr_orders`, `pos_refunds`, `loyalty_transactions` | Gộp hai bill chưa thu cùng đơn; tiền và điểm chỉ ghi khi xác nhận; dashboard và CSV đếm từng khoản thu theo ngày trả | Sau tách bill khách được hướng dẫn thanh toán từng bill với nhân viên; không dùng QR tổng đơn | Đơn được chọn hiển thị trạng thái đã thu và tiền hoàn |
| Phân ca, chấm công, ca tiền và phân quyền | `pos_shift_schedules`, `pos_attendance`, `pos_cash_shifts`, `pos_roles` | Quầy quản lý nhân viên; cầm tay chấm công; két quầy chỉ mở khi bill tiền mặt đã thu | Không hiển thị dữ liệu nội bộ | Không hiển thị |
| License POC 4 nhóm 4 ký tự | `pos_license_poc` hash + bốn ký tự cuối | Chủ tiệm thấy `ACTIVE_POC`/`EXPIRED_POC`; máy vẫn bán vì license chỉ mô phỏng | Không giới hạn đặt món | Không giới hạn phiên hiển thị |
| Chọn thiết bị in, két, máy quét | `bridge/config.local.json` và nhật ký `bridge/jobs.local.json` trên Windows quầy; cài riêng máy SUNMI trong APK | Chủ tiệm sửa ở **Thiết bị** sau khi ghép; quét HID nhập bàn, đơn, SKU, hội viên; LAN TCP chỉ nhận IP cấu hình | Không kết nối thiết bị quầy | Chỉ mở trang được cấp token ghép, không nhận job in |

Quầy và QR đang mở hỏi lại catalog khoảng 10 giây; màn hình thứ hai đọc snapshot khoảng 3 giây. D1 kiểm giá, tồn, voucher và quyền khi ghi nên màn hình chưa kịp tải lại không quyết định tiền phải thu. Worker/máy phải có Internet khi ghi; không có đồng bộ đơn từ app offline cũ. Token mật khẩu POS, GitHub D1 API token và khóa ký Android không lưu trong cấu hình quán D1; giữ ở Cloudflare/GitHub hoặc trên máy tương ứng.
