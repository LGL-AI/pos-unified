# Kiểm thử Lotus POS Unified 2.6.0-rc.4 · 2026-09-26

- `npm test`: **305/305 phép thử đạt** trên Node/SQLite bộ nhớ, trong đó [ma trận 156 thao tác chéo có tên](tests/cross-ui-70.test.mjs) kiểm 4 UI, thêm [80 case thao tác khách QR](tests/customer-journey-80.test.mjs) chia thành tám nhóm và ba bài kiểm cổng triển khai D1. Các bài QR kiểm thực qua API Worker, trong đó 10 case chạy JavaScript giao diện QR trong môi trường DOM mô phỏng. Fixture cũ kích hoạt lại 13 món demo để tiếp tục kiểm thử tương thích cùng 85 SKU Echo Coffee. Bốn bài kiểm thử nâng cấp 0009 vẫn khởi tạo 0001–0008 trước, rồi mới áp 0009 và các migration tiếp theo.
- Đường đi quan trọng sau 0009: món đang bật vẫn bán được trên QR/POS cầm tay/quầy khi tồn vật lý bằng 0; đơn chỉ giảm **ước tính**, không ghi đè tồn nhập/kiểm kê. Chỉnh giá/voucher/bàn/thuế/ngân hàng trên UI → đơn mới đọc mới, đơn cũ giữ snapshot; QR tạo đơn, POS nhận và thêm món **chưa tạo phiếu bếp**, chỉ sau khi thu tiền mới tạo phiếu; tách và gộp bill chưa thu, ghi thanh toán từng bill một lần; màn hình thứ hai và hội viên đọc kết quả D1. Mã chuyển khoản dùng mã đơn đầy đủ, mật khẩu hội viên ban đầu là số điện thoại.
- Đã thử phân quyền quản lý tiệm, chỉnh logo/tên/bàn, scanner HID tra cứu mã bàn/đơn/SKU/hội viên và đường nhận mã LAN TCP trong cầu in, giao thức ESC/POS và TSPL, két chỉ mở bill tiền mặt; hóa đơn đã in không tự gửi lại khi kết quả không rõ. 0008 áp lần đầu, chạy lại, tiếp tục sau khi một số cột đã thêm, dừng nếu lịch sử migration sai. Health không báo D1 sẵn sàng khi 0008 mới thêm dở.
- Dashboard theo thời điểm thu tiền của **từng bill** và ngày Việt Nam; hoàn tiền thuộc ngày thực trả; thử hai bill qua ranh giới nửa đêm và hoàn tiền hôm sau. CSV giữ số hoàn là **số âm có thể cộng trong bảng tính**.
- Giao diện quầy bám sidebar/thẻ món/bảng quản lý sản phẩm-voucher/lịch tuần POC; thử tự động có tìm SKU, lọc danh mục, điều hướng QR Order và lịch ca D1. **Chưa có ảnh so sánh trình duyệt**, trạng thái thiết kế ghi `blocked` ở [design-qa.md](design-qa.md).
- UI quản lý license POC đã ẩn và thao tác điều hướng trực tiếp bị chặn; dữ liệu và API lịch sử còn trong hệ thống. Cổng triển khai mới chỉ cho deploy nếu D1 thật đã đủ 13 migration, có cấu hình quán, cột `payment_preference` và 85 SKU Echo đang bán. Bản thân ba bài test cổng dùng query giả lập; workflow mới sẽ gọi D1 thật khi được chạy có token.
- D1 Cloudflare thật, APK ký và thiết bị XP-Q200/XP-365B/máy quét tại tiệm **chưa được thử bởi bộ test này**. Bản APK Cloud cũ do người dùng đã thử SUNMI/KV804 trước đó; bản nguồn APK mới chưa biên dịch/ký vì thiếu khóa UAT và Android SDK. Cầu in LAN/USB cần kiểm trực tiếp giấy, tem, lệnh két và quyền truy cập mạng nội bộ trước khi quầy sử dụng. License `XXXX-XXXX-XXXX-XXXX` vẫn POC: lưu hash/trạng thái ngày hết hạn, không khóa bán hàng hoặc liên hệ máy chủ cấp phép.
