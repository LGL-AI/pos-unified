# UAT · Lotus POS Unified v2.1.1 · 2026-09-24

`npm test`: **24/24 nhóm đạt** trên Node.js 24 với SQLite mô phỏng D1. `node --check`: Worker và hai giao diện đạt. Android biên dịch bằng SDK 35 với SUNMI printerlibrary 1.0.18; APK có chữ ký v1/v2/v3 từ **cùng khóa UAT CloudPilot trước**, versionCode 5, package `vn.lotusai.pos.cloudpilot` khác POS offline.

Đã kiểm qua API thực trong bộ thử: đặt QR/POS, thêm/hủy món, tách 4 phần thành 4 bill, thanh toán, voucher và điểm hội viên. Bổ sung thử tồn kho 0 từ migration; nhập món/nguyên liệu, thiếu kho bị chặn, trừ/cộng theo từng thay đổi, lặp idempotency không trừ hai lần, phiếu nhập chỉ nhận một lần, không cho tồn âm. Hoàn một phần/toàn bộ và hoàn bill tách được giới hạn bởi tiền đã thu, gửi lại giao dịch không hoàn lần hai, điểm/tổng chi/ngày đến cuối đồng bộ với số tiền còn lại, hàng chỉ nhập kho khi xác nhận rõ. Nhân viên với vai trò CASHIER không thể sửa kho, hoàn tiền hoặc quản lý quyền qua API; khóa tài khoản vô hiệu phiên cũ. Nút chọn món/kho/hoàn/nhân viên của POS được thực thi qua cùng Worker trong DOM giả lập.

Chủ tiệm đã xác nhận in SUNMI và KV804 hoạt động chính xác, ổn định trên bản trước ở cửa hàng. Bản này giữ kết nối máy in và thiết lập đã kiểm. Đã cấu hình theo ảnh do chủ tiệm gửi: Worker `pos-unified`, D1 `pos_unified`, UUID `4a07644e-6038-4482-a90f-e55c6c2ebd8d`, HTTPS production `pos-unified.lgl247-ai.workers.dev`. Ảnh D1 hiển thị **0 bảng**; ảnh Worker hiển thị **latest build failed** và công tắc production URL **off**. **Chưa** thử APK 2.1.1 trên máy thật, áp migrations lên D1 Cloudflare thật, xem nhật ký build lỗi, truy cập URL sau triển khai, chuyển khoản vào tài khoản thật hoặc trình duyệt cảm ứng thật. Chưa ghi dữ liệu hay deploy.

Trước khi bật cho khách: áp migrations `0001` tới `0005` lên đúng D1 bằng GitHub Actions thủ công, kiểm tra `d1_migrations` có 5 dòng; đặt Worker secrets, push repo và xác thực URL production đã bật, build thành công; nhập tồn món **và** nguyên liệu thực tế trước khi mở bán; thử quyền nhân viên, một đơn QR, một đơn POS, hoàn tiền, điểm sau hoàn, split 4 bill và in giấy bằng APK 2.1.1. Mật khẩu tạm sáu chữ số cần đổi trước khi mở cho nhân viên và khách sử dụng thường xuyên.

Tệp SQL gộp 5 migration đã vượt kiểm thử SQLite tại chỗ nhưng D1 Console thật trả `incomplete input: SQLITE_ERROR` sau khi người dùng dán. Người dùng chạy câu SELECT chỉ đọc xác nhận **không có bảng hoặc trigger ứng dụng nào**; không cần khôi phục DB. Tệp gộp **không còn được dùng**. Đã thêm workflow GitHub Actions thủ công để chạy từng migration với Wrangler và kiểm tra lịch sử, bọc CASE trong các câu SELECT của trigger để tránh lỗi phân tách SQL. **Chưa** áp migration hoặc xem kết quả GitHub Actions trên D1 thật.
